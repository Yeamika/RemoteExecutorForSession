# OpenCode

OpenCode can use REFS through the local OpenCode integration.

In this workspace, prefer using our own adjacent OpenCode worktree instead of treating OpenCode as an external black box. The neighboring OpenCode source tree can host REFS bridge code, tests, and local runtime wiring while this repo keeps the REFS backend and MCP behavior focused.

